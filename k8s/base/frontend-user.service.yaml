apiVersion: v1
kind: Service
metadata:
  name: deepface-frontend-user
  namespace: deepface
spec:
  selector:
    app: deepface-frontend-user
  ports:
    - name: http
      port: 80
      targetPort: 80

