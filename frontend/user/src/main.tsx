// User mở trang chấm công
// → bấm Start Camera
// → camera bật
// → frontend chụp frame mỗi 1 giây
// → gửi ảnh lên backend
// → backend detect mặt + extract embedding
// → backend search Qdrant
// → backend trả kết quả
// → frontend hiển thị thành công/thất bại