// - Chọn ảnh từ máy
// - Preview ảnh trước khi upload
// - Gửi ảnh bằng FormData
// - Hiển thị loading khi upload
// - Sau khi upload xong gọi lại danh sách ảnh

// Chọn ảnh
// → preview
// → bấm Upload
// → gọi faceImageApi.upload(employeeId, files)
// → reload face image list

// UI nên có : [Choose images]
// Preview ảnh
// [Upload]
