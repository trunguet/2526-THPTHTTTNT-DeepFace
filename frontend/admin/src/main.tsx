// admin đăng nhập
// → vào dashboard
// → tạo nhân viên
// → vào chi tiết nhân viên
// → upload ảnh khuôn mặt
// → backend lưu ảnh vào MinIO
// → worker trích xuất vector bằng DeepFace
// → lưu vector vào Qdrant
// → frontend hiển thị trạng thái ảnh: pending / processing / processed / failed
// → admin xem lịch sử chấm công


// - Gửi ảnh lên backend
// - Hiển thị ảnh đã upload
// - Hiển thị trạng thái xử lý ảnh
// - Hiển thị log nhận diện